[pytest]
addopts = --maxfail=1
pythonpath = src
filterwarnings =
    error
    ignore:The `deflate` function is deprecated and will be removed in future versions.:DeprecationWarning
    ignore:urllib3.*or chardet.*doesn't match a supported version
